<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Toko Online Saya</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Inter Font -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            @apply bg-gray-100 text-gray-800;
        }
        .cart-icon {
            position: relative;
        }
        .cart-count {
            @apply absolute top-0 right-0 -mt-2 -mr-2 flex h-5 w-5 items-center justify-center rounded-full bg-red-500 text-xs font-bold text-white;
        }
        .product-image {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }
    </style>
</head>
<body class="antialiased">

    <!-- Header -->
    <header class="bg-white shadow-lg sticky top-0 z-50">
        <nav class="container mx-auto px-4 py-4 flex justify-between items-center">
            <a href="#" class="text-2xl font-bold text-gray-900">TokoKita</a>
            <div class="flex items-center space-x-4">
                <button id="cart-button" class="cart-icon p-2 rounded-full hover:bg-gray-200 transition">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 3h1.386c.51 0 .955.343 1.054.853L4.5 9h15m-16.5 0v10.5a1.5 1.5 0 001.5 1.5h15a1.5 1.5 0 001.5-1.5V9m-16.5 0h16.5m-9 0h.008v.008H12V9zm2.25 0h.008v.008H14.25V9zm-4.5 0h.008v.008H9.75V9z" />
                    </svg>
                    <span id="cart-count" class="cart-count hidden">0</span>
                </button>
            </div>
        </nav>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold text-center mb-8">Produk Pilihan Kami</h1>

        <!-- Product Grid -->
        <div id="product-grid" class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
            <!-- Product cards will be inserted here by JavaScript -->
        </div>
    </main>

    <!-- Cart Modal -->
    <div id="cart-modal" class="fixed inset-0 bg-black bg-opacity-50 hidden z-50 items-center justify-center p-4">
        <div class="bg-white rounded-xl shadow-2xl w-full max-w-lg p-6 relative">
            <button id="close-modal-button" class="absolute top-4 right-4 text-gray-500 hover:text-gray-900">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>
            <h2 class="text-2xl font-bold mb-4">Keranjang Belanja</h2>
            <div id="cart-items" class="space-y-4 max-h-80 overflow-y-auto pr-2">
                <!-- Cart items will be inserted here by JavaScript -->
            </div>
            <div id="empty-cart-message" class="text-center text-gray-500 mt-4">
                Keranjang Anda kosong.
            </div>
            <div class="mt-6 pt-4 border-t border-gray-200">
                <div class="flex justify-between items-center font-bold text-lg">
                    <span>Total:</span>
                    <span id="cart-total">Rp 0</span>
                </div>
            </div>
        </div>
    </div>

    <!-- JavaScript for E-commerce Logic -->
    <script>
        // Array of products
        const products = [
            { id: 1, name: "Headphone Nirkabel", price: 1500000, image: "https://placehold.co/400x400/22c55e/white?text=Headphone" },
            { id: 2, name: "Jam Tangan Pintar", price: 2500000, image: "https://placehold.co/400x400/ef4444/white?text=Smartwatch" },
            { id: 3, name: "Keyboard Mekanik", price: 800000, image: "https://placehold.co/400x400/3b82f6/white?text=Keyboard" },
            { id: 4, name: "Mouse Gaming", price: 500000, image: "https://placehold.co/400x400/a855f7/white?text=Mouse" },
            { id: 5, name: "Kamera Digital", price: 5500000, image: "https://placehold.co/400x400/f97316/white?text=Kamera" },
            { id: 6, name: "Speaker Bluetooth", price: 900000, image: "https://placehold.co/400x400/14b8a6/white?text=Speaker" },
            { id: 7, name: "Laptop Ultrabook", price: 12000000, image: "https://placehold.co/400x400/94a3b8/white?text=Laptop" },
            { id: 8, name: "Monitor 4K", price: 7000000, image: "https://placehold.co/400x400/60a5fa/white?text=Monitor" }
        ];

        // Global state for the cart
        let cart = [];

        // DOM elements
        const productGrid = document.getElementById('product-grid');
        const cartButton = document.getElementById('cart-button');
        const cartModal = document.getElementById('cart-modal');
        const closeModalButton = document.getElementById('close-modal-button');
        const cartItemsContainer = document.getElementById('cart-items');
        const cartTotalSpan = document.getElementById('cart-total');
        const cartCountSpan = document.getElementById('cart-count');
        const emptyCartMessage = document.getElementById('empty-cart-message');

        // Function to render product cards
        function renderProducts() {
            productGrid.innerHTML = '';
            products.forEach(product => {
                const productCard = `
                    <div class="bg-white rounded-xl shadow-md overflow-hidden transform hover:scale-105 transition duration-300">
                        <img src="${product.image}" alt="${product.name}" class="product-image">
                        <div class="p-4">
                            <h3 class="text-lg font-semibold">${product.name}</h3>
                            <p class="text-gray-600 my-2">Rp ${product.price.toLocaleString('id-ID')}</p>
                            <button onclick="addToCart(${product.id})" class="mt-2 w-full bg-blue-600 text-white py-2 px-4 rounded-lg hover:bg-blue-700 transition-colors">
                                Tambahkan ke Keranjang
                            </button>
                        </div>
                    </div>
                `;
                productGrid.innerHTML += productCard;
            });
        }

        // Function to add a product to the cart
        function addToCart(productId) {
            const product = products.find(p => p.id === productId);
            const existingItem = cart.find(item => item.id === productId);

            if (existingItem) {
                existingItem.quantity += 1;
            } else {
                cart.push({ ...product, quantity: 1 });
            }
            updateCartUI();
        }

        // Function to remove a product from the cart
        function removeFromCart(productId) {
            cart = cart.filter(item => item.id !== productId
